# Princess Castle Flight

Um jogo casual de voo em estilo livro de histórias, construído com React, TypeScript, Vite e Babylon.js.

## Desenvolvimento local

```bash
pnpm install
pnpm dev
```

A publicação é feita automaticamente no GitHub Pages a cada push na branch `main`, por meio do workflow em `.github/workflows/deploy.yml`.
